# SMTP Client

## Setup

Create virtual environment:

python -m venv .venv

Install dependencies:

pip install python-dotenv


## Configuration

Create .env:

SMTP_EMAIL=
SMTP_PASSWORD=
SMTP_RECEIVER=


## Run

python src/main.py